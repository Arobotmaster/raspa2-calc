# RASPA 高通量计算工具 v2.4.0 依赖包
# 支持 RASPA2/RASPA3 双版本

# 核心科学计算库
numpy>=1.21.0          # 数组和数值计算
pandas>=1.3.0          # 数据处理和DataFrame

# CIF文件处理和晶体学计算
gemmi>=0.5.0           # CIF文件处理和晶体学计算

# 配置和日志处理
PyYAML>=6.0            # YAML配置文件处理

# 数据导出功能
openpyxl>=3.0.0        # Excel文件支持（必需：CSV→Excel导出）
tqdm>=4.60.0           # 进度条显示（必需：高通量计算进度展示）

# 可视化和科学计算（推荐）
matplotlib>=3.5.0      # 用于等温线绘制
scipy>=1.7.0           # 用于高级科学计算

# RASPA3 专用依赖（可选，仅 RASPA3 模式需要）
# json 模块为 Python 内置，无需安装
# logging 模块为 Python 内置，无需安装
